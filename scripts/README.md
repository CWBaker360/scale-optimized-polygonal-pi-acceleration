# Verification Script

```bash
python scripts/verify_scale_optimized_polygonal_pi.py --precision 1300 --output-dir output
```

The script checks the moment conditions, stability constants, high-order
slopes, remainder bounds, dyadic chord recursion, and linear tripling seed.
